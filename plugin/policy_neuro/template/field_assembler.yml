# Mappings from target field names to their components

joint_states:      # String, the target field name
  - joint_pos      # String, component field names
  - joint_vel

proprioception:
  - gravity
  - ang_vel
  - joint_states
